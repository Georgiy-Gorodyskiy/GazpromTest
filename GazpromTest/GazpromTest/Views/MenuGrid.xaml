<Grid x:Class="GazpromTest.Views.MenuGrid"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GazpromTest.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Border BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height = "50"></RowDefinition>
                <RowDefinition ></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition ></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Row="0" Grid.Column="0" x:Name="OpenFileButton" Content="Open"  BorderThickness="2" Click="OpenFileButton_Click"></Button>
            <Button Grid.Row="0" Grid.Column="1" x:Name="SaveFileButton" Content="Save"  BorderThickness="2" Click="SaveFileButton_Click"></Button>
            <Button Grid.Row="0" Grid.Column="2" x:Name="CloseFileButton" Content="Close"  BorderThickness="2" Click="CloseFileButton_Click"></Button>
        </Grid>
    </Border>
</Grid>
